{{- $authorName := "" }}
{{- with site.Params.author }}
	{{- if reflect.IsMap . }}
		{{- with .name }}
			{{- $authorName = . }}
		{{- end }}
	{{- else }}
		{{- $authorName = . }}
	{{- end }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = site }}{{ end }}
{{- $pages := slice }}
{{- if or .IsHome .IsSection }}
	{{- $pages = $pctx.RegularPages }}
{{- else }}
	{{- $pages = $pctx.Pages }}
{{- end }}
{{- $pages = where $pages "Type" "in" site.Params.mainSections }}
{{- $pages = $pages.ByDate.Reverse }}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
	{{- $pages = $pages | first $limit }}
{{- end }}
{{- $lang := or site.Language.Locale site.Language.LanguageCode "en-us" }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
	<channel>
		<title>{{ if eq .Title site.Title }}{{ site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ site.Title }}{{ end }}</title>
		<link>{{ .Permalink }}</link>
		<description>{{ with site.Params.description }}{{ . }}{{ else }}{{ site.Title }}{{ end }}</description>
		<generator>Hugo</generator>
		<language>{{ $lang }}</language>
		{{ with site.Copyright }}
			<copyright>{{ . }}</copyright>
		{{ end }}
		{{ if and (not .Date.IsZero) (gt (len $pages) 0) }}
			<lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
		{{ end }}
		{{- with .OutputFormats.Get "RSS" }}
			{{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
		{{- end }}
		{{- range $pages }}
			{{- $html := .Content }}
			<item>
				<title>{{ .Title }}</title>
				<link>{{ .Permalink }}</link>
				<pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
				{{- with $authorName }}<dc:creator>{{ . }}</dc:creator>{{ end }}
				<guid isPermaLink="true">{{ .Permalink }}</guid>
				{{- range .Params.tags }}
					<category>{{ . }}</category>
				{{- end }}
				<description>{{ $html | transform.XMLEscape | safeHTML }}</description>
				<content:encoded>{{ $html | transform.XMLEscape | safeHTML }}</content:encoded>
			</item>
		{{- end }}
	</channel>
</rss>
